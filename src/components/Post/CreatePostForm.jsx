// Re-export the CreatePostForm from its new modular location
// This ensures backwards compatibility with existing code
import CreatePostForm from './CreatePostForm/index';

export default CreatePostForm; 